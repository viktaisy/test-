// 3. Написать функцию, принимающую один аргумент - целочисленное значение, и возвращающую информацию о том является ли число отрицательным (логический тип данных (истина/ложь)).

let c = 7;

// Условный оператор if else
if (c > 0) {
console.log('false');
} else if (c < 0) {
console.log('true');
}